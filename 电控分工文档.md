# 太阳能相变粮食干燥系统 —— 项目分工文档（电控方向）

> 本文件以**电控方向**为视角，梳理 `gym/` 项目文件夹内全部代码的实现内容与职责归属，
> 明确电控组负责哪些文件、各文件承担什么电控职能，以及与其他方向的代码协作关系。

---

## 1. 项目总体技术架构

项目是一个"太阳能 + 相变储热（PCM）+ AI 强化学习控制"的粮食干燥系统，代码分四个技术方向协同完成：

```
┌─────────────────────────────── 硬件执行层（电控） ───────────────────────────────┐
│  ESP32-S3 主控制器  ──►  风机×2 / 风门舵机 / 排粮舵机 / 水泵 / 加热片 / 直流电机   │
│     ▲  UART/串口              ▲ WiFi UDP                 ▲ I2C/GPIO                │
│  ESP32-S3 气象屏  ◄──  ESP32-C3 光照节点×4 ──  传感节点（DHT11/DS18B20/BH1750）    │
└────────────────────────────────────────────────────────────────────────────────────┘
│  决策链（算法组交付 → 电控部署）  │  能源/CFD 仿真（能源组）   │  文档/绘图辅助（各组）│
```

| 方向 | 职责定位 | 对应代码 |
|------|---------|---------|
| **电控方向（本文件）** | 嵌入式固件、传感器采集、执行器驱动、AI 模型端侧部署、触摸屏 HMI、板间通信、硬件接线与驱动调试、固件安全补丁 | `.ino`、`.cpp/.h` 固件 + 接线/引脚工具 + 补丁脚本等 |
| 算法方向 | PPO 强化学习训练、干燥仿真环境、模型转换导出 | `my_test.py`、`train_ppo.py`、`convert_model.py` 等 |
| 能源/CFD 方向 | PCM 相变物性反推、集热-放热平衡仿真、Fluent 气动仿真 | `pcm_balance_model.py`、`one_way_valve.c`、`analyze_pcm.py` |
| 文档/可视化 | 流程图、训练曲线、报告配图 | `collect_flowchart*.py`、`plot_reward.py` 等 |

---

## 2. 电控方向负责的代码与职能明细

### 2.1 主控制器固件 —— `esp32_controller/esp32_controller.ino`（约 3108 行）

**文件作用**：整台设备的"电控大脑"，运行于 ESP32-S3，承担全部电气量采集、电气负载驱动、控制策略执行与人机交互。**为电控组核心交付物**。其中：

| 电控职能 | 实现内容 |
|---------|---------|
| **电气量采集** | DHT11（GPIO4）测干燥仓温湿度、DS18B20（GPIO18）测水箱温度（带断线检测）、BH1750（I2C 0x23）测光照；读取周期 2s |
| **机构位置反馈** | 导轨碰撞开关 SW1~4、NPN 接近开关（GPIO42）读取，用于导轨/转盘限位与定位 |
| **无传感器量的状态估算** | `updateStateEstimate()` 按物理规律+上次动作估算 PCM 温度/进出口风温（虚拟传感器）；`updateDryModel()` 在集热干燥阶段做能量积分，含 PCM 固液相变焓计算（`pcmTempToEnthalpy`） |
| **风机驱动** | GPIO8/GPIO21 双 MOSFET（维可思/CS25N06）LEDC PWM 20kHz，0~100% ↔ 0~255 |
| **风门/排粮舵机** | PCA9685（I2C 0x40）CH4/CH5，MG90S 脉宽 102~512；0°=全干燥、45°=半储热、90°=全储热 |
| **水泵驱动** | GPIO6 LEDC PWM，输出 60~100%，水箱 <45℃ 禁止开启（防空转） |
| **加热片驱动** | GPIO9 MOS 数字开关，滞回 0.45~0.55 防抖动，PCM≥90℃ 强制断电 |
| **直流电机驱动** | BTS7960（RPWM GPIO3/LPWM GPIO1），COLLECT 转盘正转与刹车，含多重安全互锁 |
| **AI 推理执行** | 加载 `policy_model.h` 内嵌 TFLite 模型，5 维观测归一化，约 1s/次推理（实测 6.7ms），5 维动作映射至各执行器 |
| **模式与状态机** | 顶层模式 COLLECT（8 态子状态机，含回原点/插管/光驱干燥等）/ AUTO（AI 控制）/ MANUAL（手动）；`switchMode()` 带运行安全保护 |
| **触摸 HMI** | ILI9341 TFT 自绘界面（传感面板、模式按钮、手动控制、故障灯、干燥倒计时），触摸消抖 200ms |
| **板间通信** | Serial1（115200）与气象屏双向通信：上行 `STT/STA/SYS/FLT/ADV/LUX` 状态，下行 `WX` 天气、4 路光照、`CTL` 远程指令 |
| **安全保护/故障** | 传感器断线 FAULT 上报、导轨"压→反转→松开→停"限位、电机/舵机运行互锁、加热温度切断、模式切换保护 |

**同目录配套**：`esp32_controller/policy_model.h`（TFLite 模型 C 头文件，部署副本）、`Adafruit_PWMServoDriver.cpp/.h`（PCA9685 驱动，供舵机通道输出）。

### 2.2 联网气象屏固件 —— `weather_display/weather_display.ino` + `weather_types.h`（约 124 KB）

**文件作用**：气象/光照中继与决策显示节点（ESP32-S3 + 2.8" 带字库 TFT），兼作主控制器的"上位机显示 + 远程指令入口"。电控职责：

- **联网数据获取**：WiFi 拉取天气（纯 HTTP itboy API，公网 IP 自动定位 + 50 城市表回退）、NTP 校时；每 15 分钟自动刷新。
- **无线光照接收**：接收 4 个 ESP32-C3 光照节点 UDP 广播（端口 8266），超 10s 判离线，2×2 网格显示。
- **干燥作业建议生成**：内置分级规则表（雨雪/高湿≥85%/阴天/晴天高温/大风等），输出"建议栏"并通过 UART 下发建议码给主控。
- **汉字显示驱动**：GT24L24A2W16 字库芯片 SPI 读字模（ASCII 8×16/16×32、GBK 16×16 区段），复用 CS 脚分时访问液晶/字库。
- **与主控通信**：把天气/光照/建议经 UART 发给主控制器；可转发 `CTL` 远程控制指令。
- `weather_types.h`：定义天气分类枚举、天气数据/建议结构体（固件共用类型）。

### 2.3 无线光照传感节点 —— `gy30_sender/gy30_sender.ino`

**文件作用**：ESP32-C3 + GY-30（BH1750）光照采集发送端，最多 4 路（`NODE_ID` 1~4）。电控职责：

- 原生 I2C 驱动 BH1750（上电初始化、连续高分辨率读，`raw/1.2` lux）。
- WiFi 连接（含射频扫描诊断、失败原因码解析、低发射功率配置、断线自动重连）。
- 每秒 UDP 广播 `LUX<序号>:数值` 至气象屏，作为集热器光照输入。

### 2.4 环境温湿度传感节点 —— `dht11_sensor/dht11_sensor.ino` + `oledfont.h`

**文件作用**：ESP32-C3 + DHT11 独立温湿度采集 + 0.96" OLED（SSD1315）显示节点（调试/演示用）。电控职责：

- DHT11 读取（修正系数：湿度 −6、温度 +6），I2C 扫描诊断，OLED 底层驱动（`OLED_WR_Byte`、`OLED_Init`、ASCII 8×16/16×8 字库 `oledfont.h`）与数据显示。

### 2.5 模型部署接口文件

| 文件 | 内容与电控职责 |
|------|--------------|
| `policy_model.h`（根目录/esp32_controller 内副本） | 算法组导出、**电控负责集成**：按 `POLICY_MODEL_H` 条件编译进主控固件，作为 TFLite 模型参数（约 140 KB float32）烧录运行 |
| `DEPLOY_README.md` | 部署流程手册：训练→转换→拷贝 `policy_model.h`→Arduino 编译烧录→AI/AUTO/MANUAL 模式验证，为电控与算法交接文档 |

### 2.6 硬件接线/引脚文档工具（Python 辅助）

| 文件 | 输出 | 电控用途 |
|------|------|---------|
| `wiring_table.py` | `wiring_table.png` | ESP32-S3 全引脚接线分类表（GPIO 类型/信号/说明）+ PCA9685 通道表 |
| `plot_wiring.py` | `wiring_diagram.png` | 系统全元件接线图（主板/传感器/风机/舵机/L298N/PCA9685/TFT 布局与连线） |
| `gen_wiring_png.py` | `接线表.png` | 接线清单图（自动换行版） |
| `read_ibt2.py` | 终端文字 | 读取 IBT-2（BTS7960）电机驱动原理图 PDF，硬件资料查阅 |

> 职责说明：接线方案由电控组设计实现，上述脚本用于把接线成果固化为交付图纸。

### 2.7 固件开发/调试辅助脚本

| 文件 | 电控职责 |
|------|---------|
| `_fix_motor_guard.py` | **直接修改 `esp32_controller.ino` 的补丁工具**：为 `setDCMotor` 增加"导轨未回原点禁止启动直流电机"安全保护，并同步 COLLECT 各状态中 `railHomingDone` 标志位 —— 硬件故障修复流程脚本 |
| `weather_display/_gbk_gen.py`、`weather_display/_city_gen.py`、`_gen_gbk.py` | 中文界面字符串→GBK 字节数组、城市双编码（UTF-8/GBK）生成工具，供气象屏字库显示与城市表填充（固件配套编码工具） |
| `collect_flowchart.py`、`collect_flowchart_h.py` | 输出 `collect_flowchart.png/h.png`：把 **COLLECT 模式 8 态子状态机**固件逻辑绘制成纵向/横向流程图，作为电控逻辑设计文档配图 |

---

## 3. 电控方向在代码协作中的接口

电控不是孤立开发，与算法、能源方向存在明确代码交接：

```
算法方向（PC）                          电控方向（硬件）
my_test.py（仿真环境）                    esp32_controller.ino（端侧执行）
train_ppo.py（PPO训练）        ──►        · 5维观测=DHT11/DS18B20实测 + 虚拟传感器估算
  policy_best.pt                          · 5维动作映射到风机/风门/水泵/排粮/加热
convert_model.py                          · 观测归一化与训练端一致([100,100,80,100,40])
  policy.tflite ── policy_model.h ──►     · 每 1s 推理（TFLite ~6.7ms）
train_v34.py（一条龙导出）                · 加热滞回/温度切断等端侧安全逻辑
```
- **接收端**：电控负责把算法组交付的 `policy_model.h`（训练→转换产物）集成烧录，并保证端侧观测/归一化/动作映射与训练环境 `my_test.py` 一致（两处阈值如 `WATER_TEMP_PUMP_THRESHOLD`、加热滞回需同步修改）。
- **反馈端**：`weather_display`（天气/光照）与主控实时数据构成算法所需的"光照驱动集热"观测来源；实测数据供 `analyze_pcm.py` 反推相变参数修正训练/仿真模型。

---

## 4. 全部代码文件职责归属总表（供完整分工文档）

### 4.1 电控方向（本组成员负责，共 19 项）

| 文件 | 内容 | 电控职能归属 |
|------|------|------|
| `esp32_controller/esp32_controller.ino` | 主控制器固件 | 传感/执行/AI/HMI/通信/保护 |
| `esp32_controller/policy_model.h` | 模型头文件（副本） | AI 部署 |
| `esp32_controller/Adafruit_PWMServoDriver.cpp/.h` | PCA9685 舵机驱动 | 舵机驱动 |
| `weather_display/weather_display.ino` | 气象屏固件 | 联网/显示/中继 |
| `weather_display/weather_types.h` | 固件共用类型 | 通信协议 |
| `gy30_sender/gy30_sender.ino` | 光照节点固件 | 无线采集 |
| `dht11_sensor/dht11_sensor.ino` | 温湿度节点固件 | 传感采集 |
| `dht11_sensor/oledfont.h` | OLED 字库 | 显示驱动 |
| `policy_model.h` | 模型头文件（根目录，烧录源） | AI 部署 |
| `wiring_table.py` / `plot_wiring.py` / `gen_wiring_png.py` | 接线图/引脚表生成 | 接线设计文档 |
| `read_ibt2.py` | IBT-2 原理图查看 | 硬件资料 |
| `_fix_motor_guard.py` | 固件安全补丁脚本 | 固件维护 |
| `weather_display/_gbk_gen.py` / `_city_gen.py` / `_gen_gbk.py` | GBK/城市编码工具 | 固件配套 |
| `collect_flowchart.py` / `collect_flowchart_h.py` | COLLECT 流程图 | 固件逻辑文档 |
| `DEPLOY_README.md` | 部署手册 | 交接文档 |

### 4.2 算法方向（其他成员负责，电控不开发但作接口对接）

`my_test.py`（干燥仿真环境）、`train_ppo.py`（PPO 训练）、`train_v34.py`（训练+转换编排）、`convert_model.py`（PT→TFLite→C 头导出）、`simulate_drying.py`、`test_extreme.py`（策略评测）、`_diagnose.py`（奖励/模型诊断）、`plot_reward.py`（奖励曲线分析）。
产物：`policy_best.pt`、`policy_final.pt`、`policy.onnx`、`policy.tflite`、`policy_tf/`。

### 4.3 能源/CFD 方向（其他成员负责）

`one_way_valve.c`（Fluent UDF：单向阀 porous-jump 阻力）、`analyze_pcm.py`（实测数据反推 PCM 物性/Fluent 参数）、`pcm_balance_model.py`（PCM 储放热平衡仿真）、`pcm_light_fan_controller.py`（光驱集热周期仿真）、`pcm_switch_controller.py`（集热器吸放切换仿真）。

> 注：上述 4.2/4.3 文件虽是其他方向交付，但其中 `convert_model.py` 产出的 `policy_model.h`、`my_test.py` 中阈值参数、能源组的集热策略，均与电控固件联调，属于**跨方向协作接口**，电控组需参与联调验收。

---

## 5. 电控方向职责总结

1. **固件开发**：完成主控制器、气象屏、光照节点、温湿度节点 4 套 ESP32 固件的传感采集、电气负载 PWM/舵机/IO 驱动、机构限位互锁与状态机控制。
2. **AI 端侧落地**：将算法组训练好的 TFLite 策略集成进主控，完成观测对齐、动作映射与端侧安全包装（滞回、超温切断、温度门控）。
3. **人机交互与联网**：TFT 触摸界面、气象/光照信息展示、干燥作业建议、板间 UART 与 UDP 通信。
4. **电气设计与文档化**：全系统 GPIO 接线、驱动电路（MOSFET/PCA9685/BTS7960）、接线图/引脚表图纸输出。
5. **调试与维护**：IBT-2 等硬件资料分析、固件安全补丁脚本、故障排查工具。
